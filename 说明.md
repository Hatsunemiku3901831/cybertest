# cybertest — AI 驱动的安全测试 Agent 工作空间

一个面向授权渗透测试、漏洞验证、资产探测、Web/API 安全测试和逆向工程的工程化 Agent 工作空间。让 Codex 或 Claude Code 按统一项目手册完成信息收集、漏洞发现、证据闭环与报告归档，覆盖从侦察到复盘的完整安全测试生命周期。

---

## 项目概况

| 能力面 | 当前事实源 |
|--------|------------|
| Agent 入口与任务路由 | `AGENTS.md`、`CLAUDE.md`、`agent/AGENT.md` |
| 安全测试与逆向方法 | `agent/skills/`、`hack-skills/skills/` |
| 结构化验证契约 | `agent/schemas/`、`agent/tactics/`、`agent/cases/` |
| 动态能力与证据 | `agent/capabilities/`、`tool/cybertest_core/adapters/` |
| 经验与复盘 | `agent/memory/`、`agent/retrospectives/` |
| 工具与扫描管线 | `tool/`、`tool/cybertest_core/` |
| 自动化质量门禁 | `tests/`、`tool/release_gate.py`、`.github/workflows/` |

---

## 核心理念

### 1. Agent 自主执行，而非 LLM 链编排

与传统安全平台不同，cybertest 不依赖多层 LLM chain 或 Agent 间复杂的消息传递。Codex 或 Claude Code 通过各自入口转入统一项目手册（`agent/AGENT.md`），自行规划、运行命令、修改文件并验证结果。`primary_agent`、`pentester`、`coder` 等角色仅作为工作模式参考，而非实际调用的中间层。

```
用户指令 → AGENTS.md / CLAUDE.md → agent/AGENT.md → 按需加载 skills/ → 执行与验证 → 按任务类型归档
```

### 2. 工程化、可追溯、可接手

授权渗透测试、漏洞验证、安全扫描、资产探测和目标信息收集任务按照固定目录结构归档；普通代码、文档和维护任务不自动创建任务目录：

```
tasks/YYYY-MM-DD-HHMM-short-task-name/
├── inputs/           # 授权范围、目标清单、账号/测试材料
├── notes/log.md      # 唯一主日志（决策链、命令、checkpoint）
├── outputs/          # 资产清单、候选队列、机器归档和接手文档
├── vulnerability-archive.md # 唯一主漏洞和风险总账
├── evidence/         # 原始证据（HTTP/screenshot/scan/burp/js）
├── reports/          # 阶段性/正式报告
├── temporarytool/    # 任务专用临时脚本
└── retrospective.md  # 匿名化复盘
```

关键设计原则：
- **单一主文档**：每个任务只有一个日志、一个资产清单、一个漏洞归档，不产生分散副本
- **AI 可接手**：`outputs/agent-handoff-pentest-status.md` 面向新窗口/其它 AI，写清当前状态、已做/未做、优先下一步和已知失败路径
- **证据自包含**：漏洞归档正文内必须包含请求方法、路径、认证状态、响应关键字段和复现条件，不依赖外部文件

### 3. 经验持续蒸馏

安全任务结束时写入匿名化复盘。只有用户明确要求时，才把指定复盘蒸馏为 pattern/tactic/full memory；自动化只能生成晋升建议，不会直接改写稳定 skill。三级蒸馏体系：

| 级别 | 目录 | 说明 |
|------|------|------|
| **Pattern Memory** | `agent/memory/pattern/` | 从 2-5 个相近任务提取的高频模式，标记 draft/active/stable |
| **Tactic Memory** | `agent/memory/tactic/` | 经过多次验证的稳定打法，可作为默认策略或停止规则 |
| **Full Distillation** | `agent/memory/full/` | 跨大量任务的全局性结论，标记覆盖范围和关键发现 |

### 4. 工具链归一化

`tool/` 中的核心包装器优先输出机器可读 JSON；授权网络工具显式要求 `--authorized`。具体参数、异步能力和续跑方式以各工具的 `--help` 为准，不假定所有包装器具有相同接口。跨候选评分、路由、schema 校验、动态 adapter 和证据归一化的可复用逻辑集中在 `tool/cybertest_core/`。

---

## 项目起源与设计哲学

### 为什么会有这个项目？

在实际安全测试中，有三个反复出现的痛点：

1. **工具碎片化**：nmap、nuclei、ffuf、sqlmap 等工具各有各的输出格式，人肉解析耗时且易遗漏
2. **经验流失**：每次测试的发现、判断、绕过技巧随对话结束而消失，下次遇到类似场景从零开始
3. **AI 安全审查过度拦截**：通用 AI 助手在遇到真实域名/IP 时自动触发安全警告，即使目标已明确授权

cybertest 逐一解决这些问题：
- **工具归一化**（解决碎片化）→ 核心包装器输出机器可读 JSON，并由 pipeline 统一编排
- **经验蒸馏系统**（解决经验流失）→ 复盘 → memory → skill 的晋升管道
- **先例文件防线**（解决过度拦截）→ `precedent-auth.md` 翻转授权默认值

### 四层先例防线

项目通过四个"先例文件"确保 AI 行为可控：

```
precedent-auth.md                              → 授权预声明
agent/skills/precedent-reverse.md              → 逆向操作先例
agent/skills/precedent-pentest.md              → 渗透操作先例
agent/policies/agent-obedience-engineering.md  → 执行自检与借口反驳表
```

`precedent-auth.md` 的核心设计是翻转 AI 的默认假设——从"假设未授权，直到证明已授权"变为"假设已授权，除非用户说不是"。这对于专业安全工具是正确的默认值，避免了每次提到目标都被 AI 追问授权的低效循环。

---

## 目录结构详解

```
cybertest/
│
├── AGENTS.md                          # Codex 根入口（指向 agent/AGENT.md）
├── CLAUDE.md                          # Claude Code 精简入口
├── README.md                          # 项目介绍与快速使用指南
├── 说明.md                            # 当前详细说明
├── program.md                         # 脚本开发、重构和代码审查标准
├── precedent-auth.md                  # 授权预声明（AI 安全审查前 MUST 读取）
│
├── agent/                             # ⭐ Agent 核心系统
│   ├── AGENT.md                       # 项目级稳定操作手册与一级路由
│   ├── skills/                        # 按需加载的工作流与专项方法
│   │   ├── security-testing.md        # 授权安全测试统一主入口
│   │   ├── webskill.md                # Web/API 渗透测试执行流程（历史兼容）
│   │   ├── hack-skill.md              # HackSkills 渗透测试方法论路由
│   │   ├── bounty-deep-dig.md         # 赏金/ SRC 不限时深度挖掘
│   │   ├── bounty-candidate-triage.md # 赏金候选 P0-P3 攻击队列评分
│   │   ├── bounty-closure-playbooks.md# OAuth/SQLi/SSRF/IDOR 等漏洞闭合 Playbook
│   │   ├── aggressive-high-impact.md  # 高危优先 / 快速产出高影响发现
│   │   ├── medium-fast-win.md         # 中危优先 / 快速形成可报告成果
│   │   ├── nonprod-intensive-pentest.md # 非生产/停服窗口高强度专项测试
│   │   ├── ctf-web-flag-rush.md       # CTF/靶场 Web 夺旗解题
│   │   ├── 漏洞评级.md                 # 漏洞风险定级标准（强制门禁）
│   │   ├── task-decomposition.md      # 复杂任务理解、规划、拆分、动态重规划
│   │   ├── project-architecture.md    # 项目架构理解
│   │   ├── agent-roles.md             # Agent 角色（pentester/coder/reporter）参考
│   │   ├── burp-mcp.md                # Burp Suite MCP 集成（抓包/Repeater/Intruder）
│   │   ├── experience-distillation.md # 经验蒸馏与 memory 维护
│   │   ├── handoff-docs.md            # 交接文档/接手文档/资产清单生成
│   │   ├── hunt-routing.md             # candidate 到 tactic/RouteDecision 路由
│   │   ├── dynamic-evidence.md         # 动态 provider 与统一证据工作流
│   │   ├── browser-validation.md       # 真实浏览器验证
│   │   ├── js-runtime-analysis.md      # JS/CDP 运行时分析
│   │   ├── OWASPtop10.md              # OWASP Top 10:2025 Web/API 风险建模
│   │   ├── reverse-security.md        # 逆向工程总路由入口
│   │   └── reverse/                   # 逆向工程子 Skill 体系
│   │       ├── apk-reverse/           # APK/Android 逆向（jadx/apktool/Frida）
│   │       ├── binary-diff/           # 二进制补丁差分（bindiff/Diaphora/Ghidriff）
│   │       ├── firmware-pentest/      # 固件提取/仿真/漏洞利用（binwalk/unblob/EMBA）
│   │       ├── ida-reverse/           # IDA Pro/idalib MCP 逆向工程
│   │       ├── js-reverse/            # JS 逆向/前端签名/SourceMap 还原
│   │       ├── malware-analysis/      # 恶意软件分析（沙箱/YARA/反分析检测）
│   │       ├── mobile-reverse/        # iOS/Android 移动逆向（Frida/Objection）
│   │       ├── patch-diff-exploit/    # N-day/补丁差分/1day 漏洞复现
│   │       ├── pwn-chain/             # Pwn/栈/堆/内核漏洞利用链
│   │       ├── radare2/               # radare2/r2 逆向工程
│   │       └── reverse-engineering/   # 通用逆向工程（多语言/多平台/工具集）
│   ├── policies/                      # 证据、敏感数据和执行治理规则
│   ├── schemas/                       # candidate/case/tactic/route/evidence 等 schema
│   ├── tactics/                       # 结构化验证契约、负控、不变量和停止点
│   ├── cases/                         # 匿名化案例与多维索引
│   ├── capabilities/                  # 动态能力声明和 fallback
│   ├── archive/                       # 不再代表当前架构的历史文档
│   ├── memory/                        # 经验蒸馏记忆库
│   │   ├── index.md                   # 记忆索引（按标签检索）
│   │   ├── queue.md                   # 待蒸馏经验队列
│   │   ├── pattern/                   # Pattern Memory（局部高频模式）
│   │   ├── tactic/                    # Tactic Memory（战术级稳定打法）
│   │   └── full/                      # Full Distillation（全量蒸馏结论）
│   └── retrospectives/                # 任务复盘归档（匿名化）
│       ├── index.md                   # 复盘索引
│       └── TEMPLATE.md                # 复盘模板
│
├── hack-skills/                       # ⭐ HackSkills Web/API/系统渗透测试技能库
│   └── skills/                        # 按漏洞类型/技术领域组织的专题方法论
│       ├── sqli-sql-injection/        # SQL 注入专题
│       ├── xss-cross-site-scripting/  # XSS 跨站脚本专题
│       ├── ssrf-server-side-request-forgery/ # SSRF 专题
│       ├── ssti-server-side-template-injection/ # SSTI 模板注入专题
│       ├── idor-broken-object-authorization/ # IDOR/BOLA 越权专题
│       ├── jwt-oauth-token-attacks/   # JWT/OAuth 令牌攻击专题
│       ├── api-authorization-and-bola/ # API 授权与 BOLA 专题
│       ├── waf-bypass-techniques/     # WAF 绕过技术专题
│       ├── request-smuggling/         # HTTP 请求走私专题
│       ├── deserialization-insecure/  # 反序列化漏洞专题
│       ├── windows-privilege-escalation/ # Windows 提权专题
│       ├── linux-privilege-escalation/ # Linux 提权专题
│       ├── active-directory-*/        # AD/域渗透专题（Kerberos/证书服务/ACL）
│       ├── container-escape-techniques/ # 容器逃逸专题
│       ├── llm-prompt-injection/      # LLM Prompt 注入/AI 安全专题
│       ├── race-condition/            # 竞态条件专题
│       ├── prototype-pollution/       # 原型污染专题
│       ├── graphql-and-hidden-parameters/ # GraphQL 与隐藏参数专题
│       └── ...                        # 更多按需加载的专题
│
├── tool/                              # ⭐ 可复用安全测试工具与核心模块
│   ├── scan_pipeline.py               # 🔥 扫描管线（推荐入口）quick/full/deep
│   ├── nmap_json_scan.py              # Nmap JSON 包装器（profile + 异步 + 两轮扫描）
│   ├── masscan_json_scan.py           # Masscan 高速端口扫描（大网段测绘）
│   ├── rustscan_nmap.py               # Rustscan + Nmap 快速端口发现链
│   ├── naabu_json_scan.py             # naabu 快速全端口 SYN 候选
│   ├── nuclei_json_scan.py            # Nuclei 模板化漏洞扫描
│   ├── httpx_probe.py                 # HTTP/Web 存活探测与技术栈指纹
│   ├── katana_crawl.py                # Katana Web 爬虫（JS 解析/headless）
│   ├── subfinder_json.py              # 被动子域名枚举
│   ├── dnsx_json.py                   # DNS 基线收集（A/AAAA/CNAME/NS + Fake-IP 检测）
│   ├── tlsx_json.py                   # TLS 指纹与证书信息收集
│   ├── whatweb_json.py                # WhatWeb Web 技术栈指纹
│   ├── wafw00f_json.py                # WAF/CDN/安全代理识别
│   ├── ffuf_json.py                   # ffuf 目录/参数/Host fuzz
│   ├── arjun_json.py                  # Arjun HTTP 参数发现
│   ├── kiterunner_json.py             # Kiterunner API 路由枚举
│   ├── gf_pattern_match.py            # GF 漏洞模式 URL 分类与优先级排序
│   ├── dalfox_json.py                 # Dalfox XSS 检测
│   ├── sqlmap_safe.py                 # sqlmap 低风险 SQL 注入验证
│   ├── fofa_query.py                  # FOFA 被动公网资产查询
│   ├── url_history_collect.py         # 历史 URL 收集（waybackurls）
│   ├── sploitus.py                    # Sploitus exploit/安全工具线索检索
│   ├── trivy_json.py                  # Trivy 依赖/镜像/配置/Secret 扫描
│   ├── semgrep_json.py                # Semgrep 源码安全规则扫描
│   ├── zap_baseline.py                # ZAP 被动基线扫描
│   ├── hashcat_json.py                # hashcat 离线密码哈希破解
│   ├── gitleaks_json.py               # Gitleaks 源码 Secret 扫描
│   ├── origin_exposure_probe.py       # 源站/公网入口暴露验证
│   ├── gateway_route_classifier.py    # 网关路由响应分类
│   ├── cors_upload_chain.py           # CORS + 上传 + 公开预览风险链证据收集
│   ├── quality_gate.py                # 扫描质量门禁（完整覆盖检查）
│   ├── bounty_candidate_queue.py      # 赏金候选攻击队列（P0-P3 优先级排序）
│   ├── async_task_runner.py           # 通用异步任务管理器
│   ├── task_context_bundle.py         # 任务上下文打包为单个 Markdown
│   ├── detect_capabilities.py         # 本地 capability 只读探测
│   ├── run_dynamic_validation.py      # 显式动态验证与证据回流
│   ├── release_gate.py                # 固定顺序的离线发布门禁
│   ├── cybertest_core/                # 评分、路由、schema、adapter 和 pipeline 纯函数
│   └── nuclei-templates/              # 本地高信号 Nuclei 模板
│
├── tasks/                             # 授权安全/信息收集任务归档
│   ├── CTF/                           # CTF 夺旗归档
│   └── YYYY-MM-DD-HHMM-task-name/     # 安全/信息收集任务
│
├── temporarytool/                     # 单次任务临时脚本
├── tests/                             # 测试用例
├── require/                           # minimal/web/full/reverse 依赖 profile
└── .github/workflows/                 # 离线质量工作流
```

---

## 扫描管线 — 一键式安全侦察

`tool/scan_pipeline.py` 是整个工具链的推荐入口。它将多个安全工具编排为一条命令行管线，自动处理阶段间数据传递、异步长任务轮询、断点续跑和质量门禁。

### 三种扫描深度

| 模式 | 当前默认覆盖 | 适用场景 |
|------|--------------|----------|
| **quick** | 基础资产发现、浅层 Web 收集、线索扫描及完整离线语义链 | 首次侦察、快速摸底 |
| **full** | quick + TLS、全端口候选、Nmap、历史 URL、目录 fuzz | 正式渗透测试 |
| **deep** | full 阶段集 + 更深的 headless/JS 爬取参数 | SPA、大型 API 网关、深度挖掘 |

实际耗时取决于授权范围、网络和外部工具状态；使用 `--dry-run` 查看当前阶段计划。

### 管线阶段流程

```
subfinder → dnsx → httpx → tlsx → naabu → nmap → katana → history
    → gf → nuclei → ffuf
    → js_intel → api_contract → control_gap
    → candidate_queue → tactic_match → semantic_quality_gate
```

`browser_validate`、`burp_replay`、`js_runtime_validate` 和 `oast_check` 是显式
可选阶段，不属于 quick/full/deep 默认预设。它们只生成绑定
candidate/tactic/RouteDecision 的 `plan_status=draft`，不会自动执行真实
provider；完成计划后由 `tool/run_dynamic_validation.py` 独立执行，并在执行前
重新检查 capability、材料和 policy。

### 快速上手

```bash
# 首次侦察
./tool/scan_pipeline.py --authorized --domain example.com --mode quick

# 正式渗透
./tool/scan_pipeline.py --authorized --domain example.com --mode full

# 深度挖掘（含更深的 headless/JS 爬取参数）
./tool/scan_pipeline.py --authorized --domain example.com --mode deep

# 自定义阶段
./tool/scan_pipeline.py --authorized --domain example.com \
    --phases subfinder,httpx,katana,gf,nuclei

# 预览执行计划（不实际运行）
./tool/scan_pipeline.py --authorized --domain example.com --mode full --dry-run

# 从断点恢复
./tool/scan_pipeline.py --authorized --resume pipeline_state.json
```

---

## 工具能力矩阵

### 端口扫描与服务识别

| 工具 | 用途 | 特点 |
|------|------|------|
| `nmap_json_scan.py` | 多层次端口扫描 | 多 profile、两轮扫描和异步模式 |
| `masscan_json_scan.py` | 高速端口扫描 | 大网段候选发现，结果需后续确认 |
| `rustscan_nmap.py` | 快速端口发现 + 服务确认 | Rustscan 开口 → Nmap 版本确认 |
| `naabu_json_scan.py` | 快速全端口 SYN 候选 | 轻量级开口，需后续确认 |

### Web 侦察与指纹

| 工具 | 用途 | 特点 |
|------|------|------|
| `httpx_probe.py` | Web 存活探测 | 状态码、标题、技术栈、Web Server、跳转 |
| `katana_crawl.py` | Web 爬虫 | JS 解析、robots/sitemap、headless、表单提取 |
| `whatweb_json.py` | Web 技术栈指纹 | CMS/框架/Server/CDN/JS 库识别 |
| `wafw00f_json.py` | WAF/CDN 识别 | 识别前置 WAF/CDN/安全代理 |
| `subfinder_json.py` | 被动子域名枚举 | 多数据源，递归模式 |
| `dnsx_json.py` | DNS 基线 | A/AAAA/CNAME/NS + 私网/Fake-IP 标记 |
| `tlsx_json.py` | TLS 指纹 | 证书链、SAN、协议版本、加密套件 |

### 漏洞扫描与验证

| 工具 | 用途 | 特点 |
|------|------|------|
| `nuclei_json_scan.py` | 模板化漏洞扫描 | medium/high/critical，本地高信号模板，异步模式 |
| `zap_baseline.py` | 被动基线扫描 | OWASP ZAP，支持 Docker |
| `trivy_json.py` | 依赖/镜像/配置/Secret | 多模式（fs/image/repo/config） |
| `semgrep_json.py` | 源码规则扫描 | 自动规则配置 |
| `sqlmap_safe.py` | 低风险 SQL 注入验证 | risk=1, level=1，禁止 dump/shell |
| `dalfox_json.py` | XSS 检测 | 参数/预览/admin bot 链路 |

### 目录/参数/API 发现

| 工具 | 用途 | 特点 |
|------|------|------|
| `ffuf_json.py` | 目录/参数/Host fuzz | 默认 rate=20, threads=10 |
| `arjun_json.py` | HTTP 参数发现 | GET/POST/Header，stable/passive 模式 |
| `kiterunner_json.py` | API 路由枚举 | 适合大型 SPA/API 网关 |

### 模式匹配与线索扩展

| 工具 | 用途 | 特点 |
|------|------|------|
| `gf_pattern_match.py` | 漏洞模式 URL 分类 | P0-P3 优先级，纯 Python 实现 |
| `fofa_query.py` | 被动公网资产查询 | FOFA API，仅作线索 |
| `url_history_collect.py` | 历史 URL 收集 | waybackurls，补充攻击面 |
| `sploitus.py` | exploit 线索检索 | 按产品/CVE/服务名搜索 |

### 专项验证工具

| 工具 | 用途 | 特点 |
|------|------|------|
| `origin_exposure_probe.py` | 源站/公网入口暴露验证 | DNS + DoH + 固定 Host/SNI + 直连 IP 指纹 |
| `gateway_route_classifier.py` | 网关路由响应分类 | SPA fallback/nginx 404/Java JSON error/503 |
| `cors_upload_chain.py` | CORS + 上传风险链 | 任意 Origin 凭证化 CORS + 上传 + 公开预览 |

### 凭据与 Secret

| 工具 | 用途 | 特点 |
|------|------|------|
| `hashcat_json.py` | 离线密码哈希破解 | 字典/掩码/规则，GPU Metal 加速 |
| `gitleaks_json.py` | 源码 Secret 扫描 | 自动脱敏输出 |

### 任务管理

| 工具 | 用途 | 特点 |
|------|------|------|
| `async_task_runner.py` | 通用后台任务管理器 | 长任务 start/status/wait/list/clean |
| `task_context_bundle.py` | 任务上下文打包 | 打包为单个 Markdown，供其它 AI 接手 |
| `quality_gate.py` | 语义质量门禁 | 检查阶段覆盖、候选闭合、证据和归档一致性 |
| `bounty_candidate_queue.py` | 赏金候选攻击队列 | P0-P3 排序，可显式启用 v2 tactic 路由 |
| `detect_capabilities.py` | capability 探测 | 区分 installed/configured/reachable/healthy/available |
| `run_dynamic_validation.py` | 显式动态验证 | fresh probe、policy 门禁、Evidence Envelope 和候选回流 |
| `release_gate.py` | 离线发布门禁 | 汇总单测、链接、索引、泄密、仓库卫生和 pipeline dry-run |

---

## 技能体系

### Agent Skills（操作路由层）

`agent/skills/` 下的文件定义 Agent 的工作流程和决策规则。按任务类型加载最少量相关 skill，避免上下文膨胀。

| 技能 | 适用场景 | 核心内容 |
|------|---------|---------|
| `security-testing.md` | **安全测试总入口** | 补天式深度挖掘循环、信息收集、候选队列、ROI 分流、漏洞发现链、攻击链组合、影响边界、停止规则 |
| `hack-skill.md` | 渗透测试方法论路由 | 按漏洞类型、技术栈和阶段路由到相关 HackSkills 专题 |
| `bounty-candidate-triage.md` | 赏金候选队列评分 | P0/P1/P2/P3 优先级、ROI 评分、材料需求识别 |
| `bounty-closure-playbooks.md` | 漏洞闭合 Playbook | OAuth/OIDC/SAML、SQLi、SSRF、IDOR/BOLA、文件链、网关等专项闭合流程 |
| `aggressive-high-impact.md` | 高危优先模式 | 快速找高影响漏洞、"至少挖一个高危"、成果导向 |
| `medium-fast-win.md` | 中危优先模式 | 快速形成可报告成果、阶段性快速摸底 |
| `nonprod-intensive-pentest.md` | 非生产高强度测试 | 预发/演练/停服窗口，允许更高强度操作 |
| `ctf-web-flag-rush.md` | CTF 夺旗解题 | 靶场 Web/API 解题，禁止联网搜索答案 |
| `漏洞评级.md` | 漏洞风险定级 | **强制门禁** — 所有中危/高危必须经此复核 |
| `task-decomposition.md` | 任务规划拆分 | 复杂任务理解、分步规划、接管续跑、动态重规划 |
| `project-architecture.md` | 项目架构 | 项目结构、目录职责、构建/启动命令 |
| `burp-mcp.md` | Burp Suite MCP | 代理抓包、Repeater/Intruder、编解码、配置管理 |
| `experience-distillation.md` | 经验蒸馏 | Pattern/Tactic/Full memory 维护、复盘聚合、经验晋升 skill |
| `handoff-docs.md` | 交接文档 | 资产清单、漏洞归档、任务日志、新窗口接手入口 |
| `hunt-routing.md` | tactic 路由 | candidate、RouteContext、RouteDecision 和阻塞恢复 |
| `dynamic-evidence.md` | 动态证据 | 浏览器、重放、CDP、抓包、OAST 和 CLI 的统一证据契约 |
| `browser-validation.md` / `js-runtime-analysis.md` | 运行时验证 | 真实浏览器、JS/CDP 与 SourceMap/Hook 观察 |
| `OWASPtop10.md` | 风险建模 | OWASP Top 10:2025 Web/API 风险分类与测试思路 |
| `reverse-security.md` | 逆向总入口 | 路由到匹配的逆向子 Skill |

### HackSkills（渗透测试方法论层）

`hack-skills/skills/` 按漏洞类型和技术领域组织专题方法论，按当前现象选择性加载，覆盖：

**Web/API 漏洞**：SQLi、XSS、SSRF、SSTI、CSRF、IDOR/BOLA、JWT/OAuth、SAML、GraphQL、WebSocket、请求走私、HTTP 参数污染、Host Header 攻击、CRLF 注入、CORS 配置错误、CSP 绕过、开放重定向、文件上传、路径穿越/LFI、命令注入、表达式语言注入、JNDI 注入、NoSQL 注入、XXE、反序列化、类型混淆、原型污染、竞态条件、Web 缓存欺骗、点击劫持、CSV 公式注入、邮件头注入……

**认证与授权**：认证绕过、业务逻辑漏洞、API 认证与 JWT、401/403 绕过、OAuth/OIDC 配置错误……

**系统渗透**：Windows/Linux 提权、Windows/Linux 横向移动、Windows AV 规避、macOS 进程注入、macOS 安全绕过、AD Kerberos 攻击、AD 证书服务攻击、AD ACL 滥用、NTLM Relay/Coercion、容器逃逸、K8s 渗透、沙箱逃逸、反向 Shell……

**逆向工程**：代码混淆/反混淆、反调试、二进制保护绕过、VM/字节码逆向、符号执行、格式字符串利用、栈溢出/ROP、堆利用、内核利用、智能合约漏洞、DeFi 攻击模式、Android/iOS 渗透技巧、移动 SSL Pinning 绕过……

**密码学**：经典密码分析、哈希攻击、对称密码攻击、RSA 攻击、格密码攻击……

**新兴领域**：LLM Prompt 注入、AI/ML 安全、DNS 重绑定、供应链安全（依赖混淆）、不安全的源码管理……

### 逆向工程子 Skill 体系

`agent/skills/reverse/` 下的子 Skill 覆盖 APK、二进制、固件、移动端、恶意软件和 Pwn 等场景；具体参考资料按子 Skill 需要加载：

| 子 Skill | 覆盖内容 |
|----------|---------|
| `apk-reverse/` | APK 解包/重打包、smali 修改、jadx 反编译、Frida Hook、签名绕过、root 检测绕过、证书校验绕过 |
| `binary-diff/` | bindiff/Diaphora/Ghidriff 二进制补丁差分、符号迁移、跨版本函数匹配 |
| `firmware-pentest/` | binwalk/unblob 固件提取、squashfs/UBI/JFFS2 文件系统、QEMU 全系统仿真、Firmadyne/FAT 模拟、EMBA 自动化分析 |
| `ida-reverse/` | IDA Pro/idalib MCP 交互、函数重命名、类型恢复、脚本自动化 |
| `js-reverse/` | JS 混淆还原、SourceMap 恢复、签名/加密参数提取、AST 反混淆、CDP 调试 |
| `malware-analysis/` | 沙箱编排（CAPE/Joe Sandbox）、YARA/Sigma 规则、反分析检测识别、IOC 提取、FLOSS/pe-sieve |
| `mobile-reverse/` | iOS/Android 双平台、Frida/Objection 动态插桩、SSL Pinning 绕过、越狱检测绕过、Frida Gadget 免 Root 注入 |
| `patch-diff-exploit/` | Patch Tuesday 工作流、N-day/1day 漏洞复现、补丁差分、根因分析与 PoC |
| `pwn-chain/` | 栈溢出/ROP/ret2libc、堆利用（tcache/fastbin/unsorted bin）、内核 pwn（SMEP/SMAP/KASLR/KPTI）、pwntools/GEF/pwndbg |
| `reverse-engineering/` | 通用逆向方法论、多语言（Go/Rust/C++/Python/.NET）、多平台（ELF/Mach-O/PE/WASM）、工具集、AI 辅助逆向 |
| `radare2/` | radare2/r2 命令行逆向、二进制分析、反汇编 |

---

## 漏洞评级体系

`agent/skills/漏洞评级.md` 是**强制门禁**，不是可选参考。漏洞评级分为高危、中危、低危三档。

### 评级复核最低要求

每条漏洞/风险的评级复核必须至少回答以下问题：

1. 是否具有实际危害？
2. 是否命中"不予奖励/无实际危害/证据不足"规则？
3. 证据是否闭合到真实业务影响？
4. 是否依赖过强前置条件？
5. 为什么不是更低等级？
6. 为什么不是更高等级？
7. 最终等级对应标准的哪一条？

任一问题无法回答 → 结论降级为候选或受限未验证。

### 评级状态流转

```
discovered → triaged → plan_ready → verifying → observed → confirmed
                                      ├────────→ false_positive
                                      ├────────→ downgraded
                                      ├────────→ no_impact
                                      ├────────→ out_of_scope
                                      └────────→ archived
           → blocked_need_material → (材料补齐) → verifying
```

缺少 `rating_review` 复核记录的条目只能标记为 `verifying`、`blocked_need_material`、`info` 或 `undetermined`，不得标记为 `confirmed`，不得进入正式报告中危/高危章节。

---

## 赏金候选攻击队列

赏金/Bug Bounty/SRC 任务必须维护候选攻击队列。候选来源覆盖：

```
子域名 → 存活 Web → 端口服务 → VHost/Host-SNI → 重定向链 → 目录爆破
→ URL 爬取 → 历史 URL → JS chunk/source map → API base → 前端路由
→ Swagger/OpenAPI → Actuator/health/config → 登录/SSO/OAuth 配置
→ 移动端 API → 对象存储链接 → 上传/下载/导入/导出接口
→ test/pre/dev/staging 环境
```

### 优先级定义

| 优先级 | 漏洞类型 | 策略 |
|--------|---------|------|
| **P0** | SQLi、RCE、SSTI、SSRF（可达内网）、账号接管、OAuth/OIDC/SAML 缺陷、IDOR/BOLA（核心业务对象）、文件链 RCE、API 网关绕过、测试环境连生产数据 | 立即定向验证 |
| **P1** | SSRF（不可达内网）、LFI、IDOR/BOLA（非核心对象）、越权（敏感功能）、存储型 XSS、盲 XSS（admin bot） | 优先验证 |
| **P2** | 反射型 XSS、CSRF（敏感操作）、开放重定向（可配合 OAuth）、信息泄露（有实际影响） | 队列中等待 |
| **P3** | 安全头缺失、版本号泄露、公开配置、无影响 CORS、401/403/404 弱线索 | 快速降级或归档 |

---

## 工作原则速览

1. **授权优先**：所有安全测试必须在明确授权范围内；执行网络探测的项目工具按 CLI 契约显式传入 `--authorized`。授权场景包括 SRC/众测平台、自有系统、付费渗透测试合约、CTF/靶场和开源项目研究。

2. **低影响验证**：默认只读、最小必要、可回滚。禁止破坏性操作、批量爆破、拒绝服务。用户声明"默认授权写入"时，允许低频、可回滚、可审计的测试账号/对象操作，成功证明一次即停止。

3. **证据闭环**：漏洞归档正文内必须自包含请求方法、路径、认证状态、HTTP 状态、关键响应字段、数量级、对照结果和复现条件。完整凭据、token、Cookie 和业务原值只进入任务 `evidence/restricted/`；常规文档使用脱敏摘要与证据引用。

4. **经验复用**：安全任务开始前按标签选择性读取 `agent/retrospectives/index.md` 和相关 memory，结束后写匿名化复盘；经验蒸馏只在用户明确要求时执行。

5. **AI 可解析输出**：核心工具优先输出稳定 JSON，pipeline、candidate、route、evidence 和 quality gate 使用版本化契约；具体字段以 CLI 输出和 schema 为准。

6. **中文优先**：所有报告、文档、注释默认使用中文生成；如需外文报告，必须由用户明确指定。

7. **身份脱敏**：渗透测试任务产出中不得出现操作系统的用户名、主机名、个人邮箱、个人 SSH 密钥路径、个人 API key 等可追溯到具体测试人员的标识信息。

8. **全风险记录**：所有风险都必须记录，包括低危、中危、信息泄露、配置缺陷和受限未验证线索。低风险保留在任务内部归档，中风险及以上写入正式报告。

9. **攻击链组合**：同一任务内的漏洞不得孤立分析；低危和中危发现也必须持续跟踪，尝试与认证缺陷、信息泄露、敏感文件下载、越权等线索组合，评估是否能形成更高影响的攻击链。

10. **高收益穷尽检查**：如果尚未发现高危/严重漏洞就准备结束，必须先自我提问复盘：资产信息收集是否完善？高收益方向是否都已尝试？是否还有未做的高收益动作？

---

## 环境要求

| 组件 | 要求 | 说明 |
|------|------|------|
| **AI Agent** | Codex 或 Claude Code | 分别从 `AGENTS.md` 或 `CLAUDE.md` 转入 `agent/AGENT.md` |
| **Python** | 3.11+ | `pyproject.toml` 声明的基础运行环境；测试使用标准库 `unittest` |
| **外部工具** | 按任务 profile 准备 | 使用 `require/install_macos.sh` 或 Windows 安装器的 detect/dry-run/profile 模式 |
| **动态 provider** | 运行时探测 | 以 `agent/capabilities/manifest.yaml` 和 fresh probe 为准，PATH 命中不等于可执行 |
| **容器与专项工具** | 可选 | 只在匹配的 profile、skill 或具体工具 `--help` 要求时准备 |

---

## 迁移到其它项目

如果把这套 Agent 系统复制到其它项目：

```
目标项目/
├── AGENTS.md          # Codex 根入口（指向 agent/AGENT.md）
├── agent/
│   ├── AGENT.md       # 项目级操作手册（替换 project-architecture.md 路径）
│   └── skills/*.md    # 按需保留相关技能
```

如果复制到其它目标项目，应把 `project-architecture.md` 替换成目标项目的结构；其余安全测试内容只能作为参考知识包，不要在没有授权的目标上执行。

---

## 免责声明

本项目的所有工具、技能和知识库仅供授权安全测试使用。使用者在任何目标上执行安全测试前，必须确保已获得合法授权。项目作者不对未授权使用、滥用或由此产生的任何后果承担责任。

---

>  **Cybertest** — 让 AI 成为你的安全测试副驾驶，从侦察到复盘，全程自主、可追溯、可积累。
